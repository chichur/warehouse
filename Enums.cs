namespace warehouse
{
    enum RefrehType
    {
        OnSelect,
        Full,
        Partially,
        OnlyEnable,
    }

    enum ReformingStates
    {
        Nothing,
        Select,
        SettingCargo,
    }
}